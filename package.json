{
  "name": "model-registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "RUN_LIVE_COMPLETIONS=1 vitest run completions",
    "typecheck": "tsc --noEmit",
    "sync": "tsx scripts/sync-models.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "dotenv": "^16.4.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
